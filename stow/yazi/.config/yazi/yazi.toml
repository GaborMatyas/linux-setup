# Yazi configuration for Bazzite - Video files open with MPV

[opener]
play = [
    { run = 'mpv %s', orphan = true, for = "unix" },
]

[open]
prepend_rules = [
    # Video files use MPV
    { mime = "video/*", use = "play" },
]
